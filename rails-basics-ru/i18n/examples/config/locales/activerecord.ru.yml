# Файлы локалей можно разделить
ru:
  activerecord:
    attributes:
      user:
        first_name: Имя

    # Переводы названий моделей
    models:
      # Плюрализация (множественное и единственное число в переводах)
      user:
        one: Пользователь
        other: Пользователи

    # Переводы ошибок валидации
    errors:
      models:
        user:
          attributes:
            first_name:
              # Имя атрибута не подставляем, оно будет вставлено автоматически
              blank: не может быть пустым
