en:
  activerecord:
    models:
      # BEGIN
      post:
        one: Post
        other: Posts      
      # END
      post/comment: Comment
    attributes:
      # BEGIN
      post:
        title: Title
      # END
      post/comment:
        body: Body

    errors:
      models:
        post:
          attributes:
            # BEGIN
            title:
              blank: can't be empty
            # END
