en:
  activerecord:
    models:
      # BEGIN
      post: Post
      # END
      post/comment: Comment
    attributes:
      # BEGIN
      post:
        title: Title
        body: Body
      # END
      post/comment:
        body: Body

    errors:
      models:
        post:
          attributes:
            # BEGIN
            title:
              blank: should be filled
            # END
